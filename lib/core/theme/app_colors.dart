import 'package:flutter/material.dart';

/// App Colors - Professional Healthcare Color Scheme
/// Primary: Teal/Turquoise (Healthcare, Trust)
/// Secondary: Deep Blue (Professional, Reliable)
/// Accent: Orange (Energy, Action)
class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF00897B); // Teal
  static const Color primaryLight = Color(0xFF4DB6AC);
  static const Color primaryDark = Color(0xFF00695C);

  // Secondary Colors
  static const Color secondary = Color(0xFF1565C0); // Deep Blue
  static const Color secondaryLight = Color(0xFF42A5F5);
  static const Color secondaryDark = Color(0xFF0D47A1);

  // Accent Colors
  static const Color accent = Color(0xFFFF6F00); // Orange
  static const Color accentLight = Color(0xFFFF9800);
  static const Color accentDark = Color(0xFFE65100);

  // Neutral Colors
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color cardBackground = Color(0xFFFFFFFF);

  // Text Colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textLight = Color(0xFFFFFFFF);

  // Success, Warning, Error
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFE53935);

  // Divider and Border
  static const Color divider = Color(0xFFE0E0E0);
  static const Color border = Color(0xFFBDBDBD);
}
